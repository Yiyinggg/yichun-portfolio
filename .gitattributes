# Auto detect text files and perform LF normalization
* text=auto

# MP4s are kept as normal Git blobs (not LFS) so GitHub Pages can serve them.
# Current deck videos are under 100 MB each (GitHub blob limit).
